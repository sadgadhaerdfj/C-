# 一、二进制读写的优点

1.二进制文件存储图形图像数据和数值数据占用的空间少，读写速度快。

2.二进制文件在内存中可以用fseek函数等，程序中处理起来比较灵活。

# 二、二进制处理及写入

函数void binarydeal(char* x,int s)；

主要思路 8位打包成char类型字符串进行储存

### 注意

写入时用char类型储存时，读取时数据可能为负数需要加上256；

用unsigned char则不会出现类似问题

# 三、读取二进制

void mydecode()

# 四、二进制储存的根本

二进制存储实际上回归了数据储存的本质，储存数据，我们只需要知道储存数据大小的上限我们就能以最小的空间存储最多的数据，所以所谓的char类型对应ascll码值对应int最终还是因为数据上限（8位最多只能到255）来确定的；

